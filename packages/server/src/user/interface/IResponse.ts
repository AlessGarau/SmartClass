export interface UserResponse {
    id: string;
    email: string;
    firstName: string;
    lastName: string;
    role: string;
    createdAt: Date;
    updatedAt: Date;
  }
  
export interface UserLoginResponse {
    id: string;
    email: string;
    firstName: string;
    lastName: string;
    role: string;
  }